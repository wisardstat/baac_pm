# BAAC PM - โครงสร้างระบบ (Agent Quick Guide)

## 🏗️ ภาพรวมระบบ
- **โปรเจค**: BAAC PM (ระบบจัดการโครงการ)
- **Framework**: Laravel 12.x
- **Database**: PostgreSQL
- **PHP**: ^8.2
- **เซิร์ฟเวอร์**: 127.0.0.1:8000

## 📁 โครงสร้างไฟล์หลัก
```
baac_pm/
├── app/
│   ├── Http/Controllers/
│   │   ├── Controller.php (Base)
│   │   └── TestController.php (ทดสอบ DB)
│   ├── Models/
│   │   └── User.php (โมเดลผู้ใช้)
│   └── Providers/
├── config/
│   └── database.php (คอนฟิก PostgreSQL)
├── database/
│   ├── migrations/ (โครงสร้าง DB)
│   └── seeders/
├── routes/
│   └── web.php (เส้นทางหลัก)
└── resources/views/ (Blade templates)
```

## 🗄️ ฐานข้อมูล
- **ชนิด**: PostgreSQL
- **Host**: 127.0.0.1:5432
- **DB Name**: baac_pm
- **User**: postgres
- **ตารางที่มี**: users, migrations, cache, jobs, sessions, password_reset_tokens

## 🛣️ เส้นทาง (Routes)
- `/` - หน้าหลัก (welcome)
- `/test-db` - ทดสอบการเชื่อมต่อฐานข้อมูล

## 🎯 ฟังก์ชันที่มีอยู่
- **TestController::testDatabase()** - ตรวจสอบการเชื่อมต่อ PostgreSQL
- **User Model** - โมเดลผู้ใช้พื้นฐาน (name, email, password)

## 🔧 คำสั่งที่ใช้บ่อย
```bash
php artisan serve           # เริ่มเซิร์ฟเวอร์
php artisan migrate         # รัน migration
php artisan make:controller # สร้าง controller
php artisan make:model      # สร้าง model
php artisan make:migration  # สร้าง migration
```

## 📋 สถานะปัจจุบัน
- ✅ Laravel 12.x ติดตั้งเรียบร้อย
- ✅ PostgreSQL เชื่อมต่อสำเร็จ
- ✅ Migration เริ่มต้นเสร็จสิ้น
- ✅ Test route พร้อมใช้งาน
- ⏳ รอการพัฒนาฟีเจอร์เพิ่มเติม

## 🚨 ข้อควรระวัง
- ใช้ parameterized queries เท่านั้น
- ตรวจสอบ null/undefined ก่อนเข้าถึง properties
- ใช้ try-catch สำหรับ async operations
- ใช้ภาษาไทยเสมอใน response
- ห้ามใช้ password plain text

## 📝 ไฟล์สำคัญสำหรับ Agent
1. [`instructions.md`](instructions.md) - คำสั่งสำหรับ AI
2. [`SETUP_INSTRUCTIONS.md`](SETUP_INSTRUCTIONS.md) - วิธีติดตั้ง
3. [`create_database.sql`](create_database.sql) - SQL สร้างตารางฐานข้อมูล
4. [`.env`](.env) - ตั้งค่าสภาพแวดล้อม
5. [`routes/web.php`](routes/web.php) - เส้นทาง
6. [`app/Http/Controllers/`](app/Http/Controllers/) - Controller
7. [`database/migrations/`](database/migrations/) - โครงสร้าง DB